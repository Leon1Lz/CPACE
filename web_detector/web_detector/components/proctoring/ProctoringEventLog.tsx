import type { ProctoringEvent } from '@/types/proctoring';
import { CheckCircle2, Flag, History } from 'lucide-react';

export function ProctoringEventLog({ events }: { events: ProctoringEvent[] }) {
  return <section className="border-t border-slate-200 px-5 py-4"><div className="mb-3 flex items-center justify-between"><h3 className="flex items-center gap-2 text-sm font-semibold text-slate-800"><History className="h-4 w-4 text-slate-400" />Activity log</h3><span className="text-[11px] text-slate-400">Review only</span></div><div className="max-h-52 space-y-2 overflow-y-auto pr-1">{events.length === 0 ? <div className="flex items-center gap-2 rounded-lg bg-slate-50 p-3 text-xs text-slate-500"><CheckCircle2 className="h-4 w-4 text-[#105C2E]" />No events recorded</div> : events.map((event) => <div key={event.id} className="flex gap-2 rounded-lg border border-slate-100 bg-slate-50/80 p-2.5"><Flag className={`mt-0.5 h-3.5 w-3.5 shrink-0 ${event.severity === 'high' ? 'text-red-500' : event.severity === 'warning' ? 'text-amber-500' : 'text-slate-400'}`} /><div className="min-w-0"><div className="flex items-center gap-2"><p className="truncate text-xs font-semibold text-slate-700">{event.type}</p><span className={`text-[9px] font-bold uppercase ${event.severity === 'high' ? 'text-red-600' : 'text-amber-600'}`}>{event.severity}</span></div><p className="mt-0.5 text-[11px] leading-4 text-slate-500">{event.description}{event.duration ? ` · ${event.duration}s` : ''}</p><time className="text-[10px] text-slate-400">{event.timestamp.toLocaleTimeString()}</time></div></div>)}</div></section>;
}
